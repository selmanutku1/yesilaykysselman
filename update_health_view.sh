#!/bin/bash
sed -i 's/<h3 className="font-bold text-sm text-gray-900 pb-2 border-b flex items-center gap-1.5">/<div className="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-3 pb-2 border-b border-gray-100">\n            <h3 className="font-bold text-sm text-gray-900 flex items-center gap-1.5">\n              <Stethoscope className="w-4 h-4 text-emerald-600" \/>\n              Aktif Revir Defteri Kayıtları\n            <\/h3>\n            <div className="relative w-full sm:w-64">\n              <Search className="w-4 h-4 text-gray-400 absolute left-3 top-1\/2 -translate-y-1\/2" \/>\n              <input\n                type="text"\n                placeholder="İsim, şikayet veya ilaç ara..."\n                value={searchTerm}\n                onChange={(e) => setSearchTerm(e.target.value)}\n                className="w-full pl-9 pr-3 py-1.5 bg-gray-50 border border-gray-200 rounded-lg text-xs focus:ring-1 focus:ring-emerald-500 outline-none"\n              \/>\n            <\/div>\n          <\/div>/g' src/components/HealthView.tsx
sed -i 's/{healthIncidents.length === 0 ? (/{filteredHealthIncidents.length === 0 ? (/g' src/components/HealthView.tsx
sed -i 's/healthIncidents.map((hi) => (/filteredHealthIncidents.map((hi) => (/g' src/components/HealthView.tsx
